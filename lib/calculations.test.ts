import{describe,expect,it}from"vitest";import{totals}from"./utils";describe("POS calculations",()=>{it("updates quantity, discounts and tax",()=>expect(totals([{productId:"1",name:"Bread",price:5000,cost:3000,qty:2,discount:500}],1000,18)).toEqual({subtotal:10000,discount:2000,tax:1440,total:9440}));it("never discounts beyond subtotal",()=>expect(totals([{productId:"1",name:"x",price:100,cost:0,qty:1,discount:300}],0,0).total).toBe(0))});
