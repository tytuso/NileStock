import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { BLOG_POSTS, blogPostBySlug } from "@/lib/blog";
export const dynamicParams = false;
export function generateStaticParams(){return BLOG_POSTS.map((post)=>({slug:post.slug}));}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const post=blogPostBySlug(slug);if(!post)return{};const url=`https://nilestock.shop/blog/${post.slug}`;return{title:post.title,description:post.description,keywords:post.keywords,alternates:{canonical:url},openGraph:{title:post.title,description:post.description,url,type:"article",publishedTime:post.published,images:["/og-image.png"]},twitter:{card:"summary_large_image",title:post.title,description:post.description,images:["/og-image.png"]}};}
export default async function BlogPostPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const post=blogPostBySlug(slug);if(!post)notFound();const url=`https://nilestock.shop/blog/${post.slug}`;const schema={"@context":"https://schema.org","@type":"Article",headline:post.title,description:post.description,datePublished:post.published,dateModified:post.published,mainEntityOfPage:url,author:{"@type":"Organization",name:"Nile AI Solutions"},publisher:{"@type":"Organization",name:"Nile AI Solutions"},keywords:post.keywords.join(", ")};return <main className="min-h-screen bg-[#f7faf8] text-[#13231c]"><header className="border-b border-[#dfe8e3] bg-white/90"><div className="mx-auto flex max-w-4xl items-center justify-between px-5 py-5"><Link href="/" className="font-bold text-emerald-800">NileStock</Link><Link href="/blog" className="text-sm font-semibold text-[#62736b]">← All guides</Link></div></header><article className="mx-auto max-w-3xl px-5 py-14"><div className="flex flex-wrap gap-3 text-xs font-semibold text-emerald-700"><span>{post.readingTime}</span><span>•</span><time dateTime={post.published}>{new Date(post.published).toLocaleDateString("en-UG",{dateStyle:"long"})}</time></div><h1 className="mt-5 text-4xl font-semibold leading-[1.08] tracking-[-.035em] md:text-6xl">{post.title}</h1><p className="mt-6 text-xl leading-8 text-[#56675f]">{post.intro}</p><div className="mt-10 space-y-10">{post.sections.map((section)=><section key={section.heading}><h2 className="text-2xl font-semibold tracking-[-.02em]">{section.heading}</h2><div className="mt-4 space-y-4 text-[17px] leading-8 text-[#405047]">{section.paragraphs.map((paragraph)=><p key={paragraph}>{paragraph}</p>)}</div>{section.bullets&&<ul className="mt-5 space-y-2 rounded-2xl border border-emerald-100 bg-emerald-50/70 p-5 text-[#274238]">{section.bullets.map((bullet)=><li key={bullet}>✓ {bullet}</li>)}</ul>}</section>)}</div><aside className="mt-12 rounded-3xl bg-[#073e31] p-7 text-white"><h2 className="text-2xl font-semibold">Try NileStock on the device you already own.</h2><p className="mt-2 text-sm leading-6 text-white/70">Start free, move to Lite when you need more products and code downloads, then upgrade as your retail operation grows.</p><Link href="/#pricing" className="mt-5 inline-flex rounded-xl bg-white px-4 py-2.5 font-semibold text-[#073e31]">See NileStock pricing</Link></aside></article><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/></main>;}
